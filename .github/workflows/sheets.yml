name: Check sheets

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sheets-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install --group dev
          python -m playwright install --with-deps chromium
          sudo apt-get update && sudo apt-get install -y poppler-utils
      - name: Check source and geometry
        run: |
          ruff check src scripts tests
          ruff format --check src scripts tests
          python scripts/check_algorithms.py --require-complete --photos
          python -m unittest discover -s tests -v
          python scripts/update_grouping.py --check
      - name: Build sheets
        run: |
          python scripts/build_sheets.py
      - uses: actions/upload-artifact@v7
        with:
          name: metro-notation
          path: output/
          if-no-files-found: error
          retention-days: 7
      - name: Check generated and published sheets
        run: python scripts/check_sheets.py output sheets
